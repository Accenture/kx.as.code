version: '3.8'
services:
  jenkins:
    image: ${KX_JENKINS_IMAGE}
    ports:
      - ${JENKINS_SERVER_PORT}:8080
      - ${JENKINS_JNLP_PORT}:50000
    container_name: jenkins
    volumes:
      - ${JENKINS_HOME}:/var/jenkins_home

