apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: confluence-server-pvclaim
  namespace: {{namespace}}
spec:
  storageClassName: gluster-heketi-sc
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
